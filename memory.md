# Project Memory

## Repository State

- Workspace root: `C:\Users\Пользователь\Desktop\dynnoCustoms`.
- As of 2026-04-25, the workspace was empty before initialization files were added.

## Product Goal

- The product is intended to validate customs document packages and detect inconsistencies across documents.
- The product is also intended to calculate estimated landed cost in Russia from route, weight, goods price, and TN VED code.
- The currently active release target agreed with the user is a `validation-only internal pipeline`; landed-cost calculation, logistics advisory, and TN VED assistance are later phases, not part of the current MVP gate.

## Current Artifacts

- `docs/product-roadmap.md` contains the initial product plan, MVP scope, architecture outline, timeline, risks, and required business inputs.
- `docs/product-policy.md` contains a concise product policy: target user, product value, MVP boundaries, decision principles, and rollout constraints.
- `Rule engine v1.txt` contains the first verified set of document-validation rules for the standard document pack.
- `docs/document-validation-spec.md` contains the first structured specification for document validation, including canonical fields, normalization rules, rule catalog, MVP priorities, and open questions.
- `docs/validation-policy.md` defines the validation-result policy for `passed`, `failed`, `skipped`, and `needs_review`, including the rule that informational skips are acceptable only when missing information is non-critical.
- `docs/field-catalog.md` defines the extraction field catalog by document type, label variants, data types, v1 requiredness, examples, and extraction notes.
- `schemas/common.schema.json`, `schemas/normalized-document.schema.json`, `schemas/document-pack.schema.json`, `schemas/validation-result.schema.json`, and `schemas/validation-report.schema.json` define the first machine-readable JSON contracts for extraction and validation flows.
- `docs/json-schema-overview.md` summarizes the role of the JSON schemas in the pipeline.
- `docs/mvp-readiness-table.md` tracks validation-only MVP readiness by sample pack, including document extraction status, rule readiness, MVP gate state, next sample runs, and open risks.
- `docs/execution-roadmap.md` contains the current execution roadmap from MVP hardening toward a production pilot, including the PostgreSQL-first runtime path, today-plan checklist, and the current sample-pack hardening priorities.
- `docs/postgresql-local-setup.md` documents the local PostgreSQL-first runtime setup flow, and `docker-compose.postgres.yml` defines a project-local PostgreSQL container with data persisted under `storage/postgres/`.
- `backend` now contains a FastAPI scaffold with health, schema registry, document-pack, mock validation, and rule-engine validation endpoints.
- `frontend` contains a React/Vite client for the internal web interface.
- `docs/web-application-architecture.md` records the recommended internal web deployment model and security rationale.
- `docs/buyer-personas.xlsx` contains the current sales-facing buyer-persona workbook with sheets for segment-level buyers/champions/blockers, role pains, and qualification signals.
- `docs/personal-brand-plan.md` contains the current six-month founder personal-brand plan focused on B2B trust, inbound demand, core channels, content pillars, publishing cadence, and KPIs.
- `docs/content-calendar-90-days.md` contains the current 13-week content calendar with weekly themes, channel mix, strong assets, CTA guidance, and 90-day content/business metrics for founder-led brand building.
- `docs/post-ideas-bank.md` contains the current 100-topic founder content idea bank grouped by pain area, buyer segment, AI/product angle, international-market angle, and suggested content format.
- `docs/ten-ready-posts.md` contains the current first batch of ten ready-to-publish founder post drafts for Telegram and LinkedIn about document-flow pain, buyer economics, AI boundaries, international-market framing, and B2B positioning.
- `.gitignore` and root `README.md` were added for repository hygiene and local setup guidance.
- `backend` now also includes in-memory `document pack` storage, file persistence into repo-local `uploads/`, and API routes to create, list, and fetch packs.
- `backend` now includes filename-based document classification, normalized-document domain models, a stub normalization pipeline, and pack-level normalization endpoints.
- `backend/src/dynno_customs_api/services/rule_engine_runner.py` implements the validation rule runner for rules `R001` through `R027`.
- `POST /api/validation/reports/{pack_id}` runs the rule engine for a document pack, auto-runs the current normalization stub when needed, returns a validation report, and updates pack status to `validated`, `needs_review`, or `failed`.
- `backend/pyproject.toml` now includes OCR-related dependencies: `pytesseract`, `Pillow`, and `PyMuPDF`.
- `backend/src/dynno_customs_api/config.py` now defines OCR settings for Tesseract command, OCR languages, PDF render DPI, OCR temp dir, and OCR output dir.
- `backend/src/dynno_customs_api/services/tesseract_ocr.py` implements the Tesseract OCR adapter for stored PDF and image documents, returning page-level raw text and OCR confidence.
- `backend/src/dynno_customs_api/services/ocr_provider.py` now contains the first OCR provider abstraction (`OcrProvider` protocol plus registry), so OCR orchestration can switch providers by configuration instead of calling Tesseract directly.
- `backend/src/dynno_customs_api/services/ocr_service.py` runs OCR for all files in a document pack and stores the latest OCR results in the in-memory document pack record.
- `POST /api/document-packs/{pack_id}/ocr` runs OCR for a document pack; `GET /api/document-packs/{pack_id}/ocr-results` returns the pack's latest OCR results.
- Completed OCR runs now persist raw text files under repo-local `storage/ocr/{pack_id}/{document_id}.txt` and expose that path as `raw_text_ref`.
- `backend/src/dynno_customs_api/services/text_extractor.py` implements the first OCR-text extractor for invoice, packing list, and bill of lading MVP fields.
- On 2026-05-05, a test document pack was created from `test-docs/invoice.pdf`, `test-docs/packing_list.pdf`, and `test-docs/bl.pdf`; uploaded files were persisted under `uploads/9314bab4-e861-4daa-abc9-84e44a654c58/`.
- The same test pack completed OCR for all three PDFs, persisted OCR text under `storage/ocr/9314bab4-e861-4daa-abc9-84e44a654c58/`, normalized all three documents with `partial` extraction status, and generated a validation report with summary `27 total`, `16 passed`, `3 failed`, `8 skipped`, `0 warnings`, and `0 needs_review`.
- On 2026-05-05, a second test document pack was created from `test-docs/invoice.pdf`, `test-docs/packing_list.pdf`, `test-docs/bl.pdf`, `test-docs/addendum.pdf`, `test-docs/coa.pdf`, and `test-docs/payment_confirmation.pdf`; pack id was `015350d1-49a0-4402-92f0-869d3b4743e3`.
- The six-file test pack completed OCR for all six PDFs and normalized all six documents with `partial` extraction status, but `addendum`, `coa`, and `payment_confirmation` normalized fields were empty because `text_extractor.py` currently implements extraction only for invoice, packing list, and bill of lading document types.
- `backend/src/dynno_customs_api/services/text_extractor.py` now extracts addendum fields (`addendum_no`, `addendum_date`, `contract_no`, `contract_date`, `seller_name`, `buyer_name`, `incoterms`, `payment_terms`), COA fields (`invoice_no`, `batch_no`, `manufacture_date`, `expiry_date`), and payment-confirmation fields (`document_presence`, party names, contract/addendum references).
- On 2026-05-29, `backend/src/dynno_customs_api/services/text_extractor.py` was expanded again to work on structured OCR text with preserved line breaks plus flat-text fallbacks; extraction coverage now includes more realistic addendum, COA, payment-confirmation, certificate-of-origin, and BL/MBL/HBL variants seen in the `тестовые доки/` baseline set.
- On 2026-05-29, `_parse_date` in `backend/src/dynno_customs_api/services/text_extractor.py` was hardened to ignore OCR-noise date matches that form invalid calendar dates instead of raising and aborting the whole normalization run.
- `backend/tests/test_text_extractor.py` covers extraction for addendum, COA, and payment confirmation OCR text in addition to invoice, packing list, and bill of lading.
- After expanding `text_extractor.py`, a six-file test pack was created from the same test documents; pack id was `7b15b940-1d0f-452f-850f-c99dc78cd8cc`.
- The updated six-file test pack completed OCR and normalization, generated a validation report with summary `27 total`, `23 passed`, `0 failed`, `4 skipped`, `0 warnings`, and `0 needs_review`, and pack status became `validated`.
- `POST /api/validation-runs` now accepts files and orchestrates intake, OCR, normalization, rule-engine validation, latest-report storage, and a workflow response containing `run_id`, `pack_id`, pack `status`, summary, grouped results, full report, and normalized documents.
- `GET /api/validation-runs/{pack_id}` returns the latest persisted validation run response for a document pack.
- `backend/src/dynno_customs_api/api/serializers.py` contains shared response serializers for normalized documents, validation reports, and grouped validation results used by the validation-run workflow.
- `backend/src/dynno_customs_api/services/validation_workflow.py` now contains the shared application workflow for intake-to-report orchestration and latest/history retrieval, so both web API routes and a future desktop shell can invoke the same backend core without duplicating HTTP-layer logic.
- `backend/src/dynno_customs_api/services/document_classifier.py` now supports broader filename normalization, common logistics-document abbreviations (`CI`, `PL`, `Add`, `BL`, `CO`, `COO`, etc.), and OCR-text fallback classification when the filename is weak.
- `backend/src/dynno_customs_api/services/tesseract_ocr.py` now preserves OCR page text as structured multi-line text based on Tesseract line metadata instead of flattening all words into one space-joined string; persisted raw OCR text now retains line breaks.
- `backend/src/dynno_customs_api/services/database.py` now initializes SQL-backed persistence tables for document packs, document files, OCR document results, normalized documents, validation reports, and validation results.
- `backend/src/dynno_customs_api/services/database.py` now also defines SQL tables for `users` and `auth_sessions`, and `backend/alembic/versions/20260618_0002_auth_schema.py` adds the matching Alembic migration.
- `backend/src/dynno_customs_api/services/document_pack_store.py` now uses a SQL-backed store for persisted document packs, document files, OCR results, and normalized documents while retaining the in-memory store class for isolated unit tests.
- `backend/src/dynno_customs_api/services/validation_report_store.py` now uses a SQL-backed store for persisted validation reports and validation results while retaining the in-memory store class for isolated unit tests.
- `backend/src/dynno_customs_api/services/auth_security.py` now implements password hashing/verification plus opaque bearer-session token generation for bootstrap auth without adding a new third-party auth dependency.
- `backend/src/dynno_customs_api/services/auth_store.py` now persists `users` and `auth_sessions` in the existing SQL database, including token-hash lookup and session revocation.
- `backend/src/dynno_customs_api/api/dependencies.py` now contains the shared bearer-session dependency for authenticated API access, so auth checks are not tied only to the auth route module.
- `backend/alembic.ini`, `backend/alembic/env.py`, and `backend/alembic/versions/20260521_0001_baseline_schema.py` now define Alembic migration support and a baseline schema revision for the current SQL persistence model.
- `backend/src/dynno_customs_api/services/migrations.py` runs Alembic migrations from application startup, replacing startup-time table creation through SQLAlchemy `create_all`.
- `GET /api/auth/bootstrap-status`, `POST /api/auth/register`, `POST /api/auth/login`, `GET /api/auth/me`, and `POST /api/auth/logout` now exist in `backend/src/dynno_customs_api/api/routes/auth.py`; the first registered user becomes `admin`, later self-registered users become `operator`, and open self-registration is controlled by `DYNNO_AUTH_OPEN_REGISTRATION`.
- `backend/src/dynno_customs_api/api/routes/document_packs.py`, `validation_runs.py`, and `validation.py` now require authenticated bearer sessions for working API access, while `health` and the bootstrap/auth routes remain usable before sign-in.
- `backend/tests/test_validation_runs.py` covers the validation-run workflow endpoint and latest-run lookup.
- `backend/tests/test_auth.py` now covers bootstrap registration, login, `me`, and logout/revocation for the new auth flow.
- `backend/tests/conftest.py` now provides `auth_headers` for route tests that need an authenticated bootstrap admin session.
- `GET /api/validation-runs` now returns persisted validation-run history summaries built from saved document packs and latest validation reports.
- `backend/tests/conftest.py` now forces a repo-local SQLite test database under `.tmp/pytest/backend-tests.db` and clears persisted tables between tests.
- `backend/tests/test_validation_report_store.py` covers latest-report persistence and retrieval from the SQL-backed validation report store.
- `backend/tests/test_validation_report_store.py` now also covers roundtrip persistence of separate `validation_results` rows attached to the latest report.
- `backend/tests/test_document_pack_store.py` now covers SQL roundtrip persistence for nested pack data including files, OCR results, and normalized documents.
- `frontend/src/App.tsx` now implements the document validation workspace: file selection, `POST /api/validation-runs` submission, API status links, run summary metrics, grouped validation results, and normalized document table.
- `frontend/src/App.tsx` now also loads validation-run history, shows saved runs in the side panel, and can reopen a previous run without rerunning OCR or validation.
- `frontend/src/App.tsx` now also renders validation result details from `observed_values` and `expected_values`, highlights `needs_review` and `skipped` states separately, and selects the initial result tab by group priority instead of a simple failed-or-skipped fallback.
- `frontend/src/App.tsx` now also renders validation-result explainability blocks: `Found` values, inferred `Missing required inputs` for common skipped-rule shapes, humanized document/field labels, and `Source snippets` from rule evidence so operators can inspect mismatches without opening the source PDF immediately.
- `frontend/src/App.tsx` now also implements a shipment-detail workspace styled after the selected `variant 4` mock: sidebar navigation plus recent shipments, top search and action bar, auto-open of the latest validation run, shipment summary metrics, ordered document-stage cards, step-focused issue review, extracted-field panel, next-action card, recent alerts, and activity timeline while preserving file selection, rerun, export, share, and history reopening flows.
- `frontend/src/App.tsx` now also renders the extracted-data panel as an operator-facing extraction review instead of a raw object dump: document fields show primary value, raw value, normalized value, source snippet, confidence, derived/page badges, and line-item rows, and the shipment header now correctly resolves exporter/importer/incoterms/route from structured field objects returned by the API.
- `frontend/src/App.tsx` now also has explicit product-state handling for the shipment workspace: dedicated `loading workspace`, `validation in progress`, retryable `API offline / empty start` entry states, and a searchable `no stages match` state with a clear-search action instead of only falling back to the generic empty panel.
- `frontend/src/lib/api.ts` defines typed API helpers for health, schema index, and validation-run creation.
- `frontend/src/lib/api.ts` now also contains typed auth helpers (`bootstrap-status`, register, login, me, logout), shared bearer-header request handling, and structured `ApiError` status propagation for frontend auth/session recovery.
- `frontend/src/lib/api.ts` now also types normalized document fields and line items as structured field records (`value`, `raw_value`, `normalized_value`, `confidence`, `text_snippet`, `derived`, `unit`, `page_no`) instead of opaque `unknown` blobs.
- `frontend/src/lib/api.ts` now includes typed `evidence` metadata on validation results for UI explainability rendering.
- `frontend/src/App.tsx` now restores auth sessions from local storage, loads validation history only after successful auth, handles expired sessions by clearing the stored token, and renders a dedicated bootstrap/login panel before the protected internal workspace is unlocked.
- `frontend/src/styles.css` defines the operational validation workspace layout, responsive report UI, and dedicated detail-card styling for `needs_review`, `skipped`, observed/expected rule values, missing-input callouts, and evidence snippet lists.
- `frontend/src/styles.css` now defines the new shipment workspace visual system for the `variant 4` direction, including glassy shell layout, sidebar navigation, KPI summary band, process-stage cards, issue and extracted-data panels, action footer, and responsive behavior for the shipment-detail screen.
- `frontend/src/styles.css` now also includes dedicated auth-panel styling plus a compact logout button so the new internal-access gate matches the existing workspace visual system.
- `frontend/src/styles.css` now also scales icon sizes, KPI icon boxes, stage-card icon circles, button heights, and spacing through CSS variables and tighter breakpoints so the shipment workspace fits better on narrower laptop/in-app-browser viewports.
- `frontend/src/styles.css` now also has a second compact-layout pass for the shipment workspace: a narrower sidebar, tighter masthead/search/profile cluster, a smaller shipment title, a thinner KPI summary band, and narrower/shorter document stage cards so more of the process row fits within laptop-width screens.
- `frontend/src/styles.css` now also has a third compact pass focused specifically on the highlighted shipment header and KPI strip: the `Shipment SHP-...` title is smaller again, and the summary band uses tighter padding, smaller numeric type, and a smaller progress ring while keeping action-button sizes unchanged.
- `frontend/src/styles.css` now also includes a visual reverse-theme pass for the left sidebar plus a looser top-right account area: the sidebar uses a dark navy gradient with white navigation, darker recent-shipment cards, and light metadata, while the `Alex Kim` profile chip in the masthead is larger and less cramped against the right edge.
- `frontend/src/App.tsx` and `frontend/src/styles.css` now also enforce a stealthier document-stage row: `Addendum` is the first stage in the validation chain, and all stage cards use a fixed equal card width/height with reserved title/message/hint rows so every document tile renders at the same size.
- `frontend/src/styles.css` now also removes visible scrollbars from the page shell and reshapes the document-stage area into a centered adaptive grid without horizontal overflow: arrows between document cards are hidden, the stage block uses a narrower centered width, and cards fill the grid columns instead of creating an inner horizontal scroller.
- `frontend/src/styles.css` now also gives the document cards enough fixed height to fully contain their footer metadata and keep the next detail section from overlapping: stage cards use `overflow: hidden` and a taller equal-height shell so the footer text stays inside the card boundary.
- The current implemented web UI and API surface remain focused on document validation; no landed-cost calculator routes or frontend calculator screen are present in the repository yet.
- The current validation workflow still runs OCR, normalization, and rule execution synchronously in the request cycle; no separate queue/worker implementation is present in the repository yet.
- `frontend/package-lock.json` was generated after installing frontend dependencies locally.
- `backend/src/dynno_customs_api/config.py` allows CORS from both `http://localhost:5173` and `http://127.0.0.1:5173` so the Vite dev server can call the API from either local URL.
- Root `.gitignore` now excludes repo-local `.env` files, including `backend/.env`, so local database credentials stay untracked.
- On 2026-06-18, a safe workspace cleanup removed repo-local cache and temp artifacts: `.cache/npm`, the old portable Node runtime `.cache/node/node-v22.11.0-win-x64`, both cached Node zip archives, `.tmp`, and `backend/.pytest_cache`, reclaiming about `307 MB` without touching `uploads/`, `storage/dynno_customs.db`, `backend/.venv`, `frontend/node_modules`, or the current portable Node runtime `22.12.0`.

## Decisions

- Start with a narrow MVP focused on one shipment per case and three core document types: invoice, packing list, and bill of lading.
- Plan the system as two modules: document validation and landed-cost calculation.
- The current product implementation is deterministic application software, not an LLM-based agent runtime; OCR, extraction, validation, and reporting currently run without any language model dependency.
- Prefer Python/FastAPI backend, React frontend, PostgreSQL, and an external OCR/document AI provider for the first working version.
- On 2026-06-18, the user confirmed that long-term product direction should expand beyond the current learned templates, but did not yet confirm a specific unknown-template handling policy such as graceful degradation.
- On 2026-06-18, the user chose a `PostgreSQL-first` path for upcoming runtime and authentication work instead of introducing auth on the default local SQLite runtime.
- On 2026-06-18, the first auth implementation was added directly on top of the existing SQL/PostgreSQL persistence layer as bootstrap-first bearer sessions (`users` + `auth_sessions`).
- On 2026-06-18, the next auth step was completed: protected working API routes now require bearer auth, the frontend now has a bootstrap/login gate and session restore flow, and `health` plus auth bootstrap endpoints remain the only pre-auth entry points used by the internal web app.
- For the first persistence step, keep report structures serialized as JSON payloads in SQL tables.
- For the second persistence step, store `document files`, `OCR results`, and `normalized documents` in separate SQL tables while still keeping per-row JSON payloads to avoid premature ORM decomposition of every nested field.
- After extending the second persistence step, store `validation results` in a separate SQL table keyed by `(report_id, rule_code)` while keeping the report header and summary payload in `validation_reports`.
- Default extraction should read the full document first; explicit page regions should be added only for fields that prove ambiguous or unstable across templates.
- The first validation specification separates `MVP Core` rules from `Phase 2` rules to reduce extraction complexity in the initial implementation.
- The extraction layer now has a document-by-document field catalog with explicit MVP Core priorities.
- The data contract is now formalized as JSON Schema around five artifacts: common definitions, normalized document, document pack, validation result, and validation report.
- The implementation direction is now an internal web application with a separate backend API and manually scaffolded frontend.
- The next code layer after scaffolding is `document intake` first, before OCR and rule execution.
- After intake, the next implemented layer is a schema-aligned normalization stub that classifies documents by filename and emits `partial` normalized documents for downstream validation flow.
- The rule-engine runner treats `hbl` as the preferred bill of lading document when both `hbl` and `mbl` are present; otherwise it uses `mbl`.
- Rule results use the existing validation statuses: `passed`, `failed`, `skipped`, and `needs_review`; summary `warnings` counts failed warning-severity results.
- Rule `R015` now uses the normalized `has_pallets` applicability signal: it passes when pallets are not applicable, warns when pallets are present but pallet weight or quantity is missing, and passes when pallet details are present.
- Rule `R004` product-name matching compares invoice, packing list, addendum, and COA product line items when available; BL `cargo_description` is excluded because BL descriptions may be shorter or more general.
- OCR endpoint execution currently runs synchronously and stores OCR result metadata in memory; raw OCR text is persisted to repo-local files.
- Document pack status now includes `ocr_completed` and `ocr_failed` in the JSON Schema.
- For the observed sample commercial invoice, `QRT-SOH` is the customs-relevant contract number, `ADD 68` is the addendum number, and `RT260004` is a Sales Contract number that is not needed for customs validation.
- Invoice unit price values like `CNY9.1000/MT` mean `9100 CNY` per metric ton.
- Extractor v1 intentionally ignores Sales Contract number `RT260004` and maps invoice unit price `CNY9.1000/MT` to `9100.0 CNY/MT` while converting `18000.00KG` to `18.0 MT` for invoice arithmetic.

## Validation Scope Known So Far

- Standard document pack currently includes: contract, addendum, commercial invoice, packing list, COA, master/house bill of lading, transport invoice, certificate of origin, and payment confirmation.
- Verified rule themes in `Rule engine v1.txt`: party-name matching, contract/invoice references, product-name matching, addendum-vs-contract dates, Incoterms consistency, invoice number matching, invoice arithmetic, packing-list weight/package logic, prepayment-to-payment-confirmation dependency, COA batch/manufacture/expiry checks, and BL-to-packing-list consistency checks.
- The structured rule catalog currently defines rules `R001` through `R027`.
- The field catalog covers canonical fields, line-item schema, label variants, and extraction priorities for all currently known document types.

## Environment Notes

- `git` is available (`git version 2.54.0.windows.1`).
- `py` launcher is available with `Python 3.13.13`.
- System `node` and `npm` remain unavailable through PATH, but portable Node.js `v22.12.0` was downloaded to repo-local `.cache/node/` and used for frontend installation/build commands with npm cache in `.cache/npm/`.
- A project-local backend virtual environment was created at `backend/.venv`.
- `backend\\.venv\\Scripts\\python -m pip install -e backend[dev]` completed successfully.
- `backend\\.venv\\Scripts\\python -m pytest backend/tests` passed with `2 passed`.
- After rule-engine runner changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `8 passed`.
- Tesseract OCR is installed at `C:\Program Files\Tesseract-OCR\tesseract.exe`.
- Tesseract version check returned `5.5.0.20241111`; available languages include `eng`, `rus`, `chi_sim`, `chi_tra`, `osd`, and `equ`.
- After installing OCR dependencies into `backend/.venv`, imports for `pytesseract`, `Pillow`, and `PyMuPDF` succeeded and `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `8 passed`.
- OCR config defaults resolved `settings.tesseract_cmd` to `C:\Program Files\Tesseract-OCR\tesseract.exe`, `settings.ocr_langs` to `eng+rus`, `settings.ocr_temp_dir` to repo-local `.tmp\ocr`, `settings.ocr_output_dir` to repo-local `storage\ocr`, and `settings.ocr_pdf_dpi` to `300`.
- After OCR config changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `10 passed`.
- After Tesseract OCR adapter changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `13 passed`.
- After OCR endpoint changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `17 passed`.
- After OCR raw-text persistence changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `18 passed`.
- After extractor v1 changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `22 passed`.
- On 2026-05-05, the backend was started with `backend\\.venv\\Scripts\\python -m uvicorn dynno_customs_api.main:app --host 127.0.0.1 --port 8000` from `backend`; `GET /api/health` returned `status: ok`.
- The 2026-05-05 validation report for the three-file test pack set pack status to `failed` because COA rules `R019`, `R020`, and `R021` failed due missing COA batch, manufacture date, and expiry date fields.
- The 2026-05-05 validation report for the six-file test pack also returned `27 total`, `16 passed`, `3 failed`, `8 skipped`, `0 warnings`, and `0 needs_review`; failures remained `R019`, `R020`, and `R021` because COA fields were not extracted despite OCR text containing `BATCH NO.: 95320172`, `MANUFACTURE DATE: MAR.08,2026`, and `EXPIRY DATE: MAR.07,2028`.
- After addendum/COA/payment extractor changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `26 passed`.
- After restarting uvicorn and rerunning the six-file validation pack, COA rules `R019` through `R023` and prepayment rule `R018` passed; remaining skipped rules were `R003`, `R005`, `R015`, and `R016`.
- After adding validation-run endpoints and shared serializers, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `28 passed`.
- After restarting uvicorn, `POST /api/validation-runs` on the six-file test set produced run id `9383b505-4499-4ece-b0dc-2e8dead12ea3`, pack id `dd91cc2f-caea-4e2d-900c-13839975593a`, status `validated`, summary `27 total`, `23 passed`, `0 failed`, `4 skipped`, `0 warnings`, and `0 needs_review`; `GET /api/validation-runs/{pack_id}` returned the same run.
- On 2026-05-14, backend dependencies were updated to include `sqlalchemy` and `psycopg[binary]`; `backend\\.venv\\Scripts\\python -m pytest backend\\tests` then passed with `31 passed`.
- Backend settings now include `DYNNO_DATABASE_URL`; the default runtime database path is repo-local `storage/dynno_customs.db`, while pytest uses repo-local `.tmp/pytest/backend-tests.db`.
- On 2026-05-15, PostgreSQL 17 was installed locally on Windows via `winget`; service `postgresql-x64-17` was started, database `dynno_customs` was created, and backend runtime was switched to PostgreSQL through local `backend/.env`.
- After the PostgreSQL switch on 2026-05-15, `GET /api/health` returned `status: ok`, and the `dynno_customs` database contained SQL-created tables `document_packs` and `validation_reports`.
- On 2026-05-21, the second persistence step was applied and verified: `dynno_customs` now also contains tables `document_files`, `ocr_document_results`, and `normalized_documents`.
- After the second persistence step on 2026-05-21, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `31 passed`.
- On 2026-05-21, persistence was extended further so `dynno_customs` now also contains table `validation_results`; the live PostgreSQL schema now includes `document_packs`, `document_files`, `ocr_document_results`, `normalized_documents`, `validation_reports`, and `validation_results`.
- On 2026-05-21, Alembic `1.18.4` was installed into `backend/.venv`, a baseline revision `20260521_0001` was added, `alembic upgrade head` successfully created the full schema in a fresh PostgreSQL database `dynno_customs_alembic_test`, and the existing local database `dynno_customs` was aligned to that baseline with `alembic stamp head`.
- After introducing Alembic on 2026-05-21, `backend\\.venv\\Scripts\\alembic.exe -c alembic.ini current` returned `20260521_0001 (head)` for the local PostgreSQL runtime, and `backend\\.venv\\Scripts\\python -m pytest tests` still passed with `31 passed`.
- On 2026-05-22, application startup was changed to run Alembic migrations; a FastAPI lifespan check against a repo-local SQLite database returned `200 ok` after applying baseline migration, and the temporary startup-check database was removed.
- On 2026-05-22, validation-run history was added end-to-end: backend history API, frontend history list, and reopening saved runs by `pack_id`.
- On 2026-05-22, `has_pallets` was added to normalized fields and schema, packing-list extraction now derives it from pallet terms, and rule `R015` now evaluates instead of always skipping when the applicability signal exists.
- On 2026-05-26, validation orchestration was refactored out of FastAPI routes into `services/validation_workflow.py`; both `/api/validation-runs` and `/api/validation/reports/{pack_id}` now use the same workflow service and shared run serializers.
- After the 2026-05-26 validation workflow refactor, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `33 passed`.
- On 2026-05-29, a baseline OCR run across `10` real document sets (`62` PDFs) under `тестовые доки/` showed `0` OCR execution failures but high filename-classification noise: `30` filename fallbacks, `34` low-classifier-confidence documents, `19` documents with no extracted fields, and `5` low-OCR-confidence documents.
- On 2026-05-29, after improving document classification, a repeat baseline run on the same `10` sets reduced filename fallbacks from `30` to `5` and low-classifier-confidence documents from `34` to `9`; the remaining dominant gap became extraction coverage (`25` documents with no extracted fields), especially for COA, bill of lading, certificate of origin, and some addendum/payment files.
- After the 2026-05-29 classifier changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `36 passed`.
- On 2026-05-29, OCR text assembly was upgraded to preserve line breaks from Tesseract page output; after this OCR-structure change, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `37 passed`.
- After the 2026-05-29 extractor expansion on top of structured OCR text, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `42 passed`.
- On 2026-05-29, a repeat baseline run across the same `10` real document sets (`62` PDFs) after extractor improvements produced `.tmp/ocr_baseline_20260529_after_extractor.json`; compared with the post-classifier baseline, `no_extracted_fields` dropped from `25` to `5`, documents with `5+` extracted fields increased from `8` to `20`, and average extracted field count rose from `1.97` to `3.63`.
- The full repeat baseline run across all `10` sets on 2026-05-29 took about `440.7` seconds end-to-end in the local test workflow, roughly `44.1` seconds per order or `7.1` seconds per PDF on average.
- On 2026-05-30, a repeated `POST /api/validation-runs` run on the six-file `test-docs` pack produced run id `2d2d2c58-5979-48d4-8fa5-dad5f02d8f05`, pack id `80701707-4ede-4e7c-a79c-29313e60b5c5`, status `validated`, and summary `27 total`, `22 passed`, `0 failed`, `1 warning`, `4 skipped`, `0 needs_review`; the full response was saved to `.tmp/validation-run-6files-rerun-20260530.json`.
- In the 2026-05-30 repeated six-file run, COA rules `R019` through `R023` all passed again with extracted values `batch_no=95320172`, `manufacture_date=2026-03-08`, and `expiry_date=2028-03-07`; the only warning-level issue was `R001` (shipper/manufacturer name mismatch).
- On 2026-06-03, frontend explainability for validation results was extended without backend changes: skipped rules can now surface inferred missing inputs such as `contract.contract_no` or `mbl.gross_weight_kg`, and result cards can display evidence snippets already returned by the API.
- On 2026-06-03, frontend explainability was refined further so the `Missing required inputs` block now renders only for `skipped` and `needs_review` results when inputs are actually missing; it no longer appears on `passed` results like `R016` where omitted fields are conditionally not required.
- On 2026-06-03, `backend/src/dynno_customs_api/services/text_extractor.py` was updated so packing-list extraction now populates `items_quantity` from bag/item counts, and MBL/HBL gross-weight extraction now covers multiline table OCR where the weight appears in `TOTAL: ...` rows before `ABOVE PARTICULARS`.
- On 2026-06-03, `backend/src/dynno_customs_api/services/rule_engine_runner.py` was updated so `R016` uses the non-pallet formula when `has_pallets = false`; pallet fields remain required only when pallets are actually present.
- On 2026-06-03, `R016` was refined further: when `has_pallets = false` but `package_type` is bag/carton/box/package (including aliases like `pkg`) and `gross_weight_kg - net_weight_kg` exceeds `300 kg`, the rule now raises a warning-level mismatch asking the operator to re-check whether pallets are actually present instead of silently treating the shipment as non-palletized.
- After the 2026-05-22 history and `R015` changes, `backend\\.venv\\Scripts\\python -m pytest tests` passed with `32 passed`, and the frontend TypeScript/Vite production build completed successfully via repo-local Node.js.
- On 2026-06-03, `frontend` verification passed via direct repo-local Node.js execution: `node.exe .\\node_modules\\typescript\\bin\\tsc -b` succeeded and `node.exe .\\node_modules\\vite\\bin\\vite.js build` succeeded; the `npm` wrapper path still remains unreliable in this shell context.
- On 2026-06-03, `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `45 passed` after the `R016` and BL gross-weight extraction changes.
- After the large gross-minus-net non-pallet warning branch was added to `R016` on 2026-06-03, `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `46 passed`.
- On 2026-06-03, a repeated `POST /api/validation-runs` run on the six-file `test-docs` pack produced run id `59904e5f-c124-48de-aaea-e38c4dfb3e9b`, pack id `d040e2bd-d285-4960-8f73-036847e22e1f`, and summary `27 total`, `24 passed`, `0 failed`, `1 warning`, `2 skipped`, `0 needs_review`; `R016` and `R024` both changed from `skipped` to `passed`, while `R003` and `R005` remained `skipped` because the pack still has no separate `contract` document.
- On 2026-06-03, a control `POST /api/validation-runs` rerun on the same six-file `test-docs` pack after adding `package/packages/pkg` to the `R016` non-pallet warning branch produced run id `9e9a290b-d8b7-43b9-994f-2827ff6de647`, pack id `73c40182-6d66-4d80-8919-31c215fa4d8b`, and the same rule summary `27 total`, `24 passed`, `0 failed`, `1 warning`, `2 skipped`, `0 needs_review`; `R016` still passed on the real bag-based sample (`18144 - 18000 = 144 kg`), while workflow `status` came back as `needs_review` because the pack still contains one warning-level rule result (`R001`).
- On 2026-06-04, `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed again with `46 passed in 4.35s`.
- On 2026-06-04, the frontend production verification passed again via repo-local Node.js `v22.12.0`: TypeScript build and `vite build` succeeded, producing `frontend/dist/` with main bundle `assets/index-DXr2k8-R.js` about `205.02 kB` before gzip.
- On 2026-06-04, a FastAPI `TestClient` readiness check was run from `backend` against the configured PostgreSQL runtime (Alembic logged `PostgresqlImpl`), `GET /api/health` returned `200`, and a six-file `test-docs` validation run produced run id `d11e43cf-4e99-4a43-bc7b-3fba12e1ffd1`, pack id `8e5f522a-25f2-446b-b624-2ed72dccb361`, pack status `needs_review`, and summary `27 total`, `24 passed`, `0 failed`, `1 warning`, `2 skipped`, `0 needs_review`; the full response was saved to `.tmp/readiness_validation_run_20260604.json`.
- In the 2026-06-04 readiness run, the only non-passed results were warning-level `R001` (`Shipper/manufacturer names do not match across documents.`) plus skipped `R003` and `R005` because the test pack still has no separate `contract` document.
- On 2026-06-04, the frontend was redesigned toward the selected `variant 4` shipment workspace and verified end-to-end: direct repo-local Node.js build succeeded again with output `frontend/dist/assets/index-BzOX5uKf.css` about `17.17 kB` and `frontend/dist/assets/index-C3BNjKbt.js` about `223.95 kB` before gzip.
- On 2026-06-04, backend and frontend local servers were started again at `http://127.0.0.1:8000/` and `http://127.0.0.1:5173/`; HTTP checks returned `200` for `/api/health` and the frontend root.
- On 2026-06-04, the in-app browser DOM snapshot confirmed that the new frontend renders the latest shipment run in the redesigned workspace: the sidebar, shipment header, metric band, six document-stage cards, issue detail panel, extracted-field panel, recent alerts, and activity timeline all rendered against live API data from shipment `SHP-8E5F522A`.
- On 2026-06-04, after responsive tuning for the shipment workspace, the frontend build succeeded again with output `frontend/dist/assets/index-CcqgapAi.css` about `18.82 kB` and `frontend/dist/assets/index-CMVoYAlh.js` about `223.95 kB` before gzip.
- On 2026-06-04, the in-app browser reload confirmed that the responsive sizing rules were active on the current narrow viewport (`599x687`): CSS variables resolved to `--icon-size: 0.92rem`, `--summary-icon-box: 40px`, and `--stage-icon-box: 52px`.
- On 2026-06-04, after a second compact-layout CSS pass, the frontend build succeeded again with output `frontend/dist/assets/index-DbsuM7GW.css` about `18.93 kB` and `frontend/dist/assets/index-6wKi56Vp.js` about `223.95 kB` before gzip.
- On 2026-06-04, the in-app browser DOM check still showed the redesigned shipment workspace rendering live shipment `SHP-8E5F522A` with all six stage cards after the compact-layout pass.
- On 2026-06-04, the current narrow in-app browser viewport (`599x687`) measured the compact layout at `Shipment` title `31.04px`, stage-card `min-height 186px`, summary icon box `36px`, stage icon box `48px`, and stage-flow width `1238px` content over `575px` visible width; this confirms the CSS compression is active, though the side-by-side Codex browser pane remains much narrower than a normal laptop-width page.
- On 2026-06-04, after the third compact CSS pass, the frontend build succeeded again with output `frontend/dist/assets/index-CpRlsWcR.css` about `18.93 kB` and `frontend/dist/assets/index-ZKBsi3B7.js` about `223.95 kB` before gzip.
- On 2026-06-04, the in-app browser reload on the current narrow viewport (`599x687`) measured the tightened highlighted areas at shipment title `26.88px`, summary strip height about `456.03px`, status-ring size `50x50px`, summary metric value font about `15.24px`, and summary label font about `12.16px`.
- On 2026-06-04, after the sidebar reverse-theme and masthead account-spacing pass, the frontend build succeeded again with output `frontend/dist/assets/index-BJsBDRgb.css` about `19.10 kB` and `frontend/dist/assets/index-BSYIkx_1.js` about `223.95 kB` before gzip.
- On 2026-06-04, the in-app browser verification confirmed the dark sidebar gradient and larger profile chip: sidebar background resolved to `linear-gradient(rgb(21, 50, 100) 0%, rgb(14, 33, 66) 100%)`, nav-item text to `rgba(236, 243, 255, 0.9)`, profile avatar to `40x40px`, profile gap to `10px`, and masthead padding to `8px 22px 8px 16px`.
- On 2026-06-04, after reordering the stage chain and hardening card sizing, the frontend build succeeded again with output `frontend/dist/assets/index-BDxlMXOD.css` about `19.68 kB` and `frontend/dist/assets/index-CVgjP1UY.js` about `223.95 kB` before gzip, and the final in-app browser DOM check confirmed the stage order `Addendum`, `Invoice`, `Packing List`, `Certificate of Analysis`, `Bill of Lading`, `Payment Confirmation`.
- On 2026-06-04, the final stage-card size verification in the in-app browser measured all six cards at the same `192px` width and `176px` height on the current viewport.
- On 2026-06-05, after the overflow-removal pass, the frontend build succeeded again with output `frontend/dist/assets/index-BAKDJK8m.css` about `19.59 kB` and `frontend/dist/assets/index-CtnGSX_o.js` about `223.95 kB` before gzip.
- On 2026-06-05, the in-app browser DOM measurement confirmed that the document-stage block no longer had horizontal overflow on the current viewport: `.stage-flow` reported `scrollWidth = clientWidth = 575` with `overflowX: visible` after being converted from an inner horizontal scroller to an adaptive grid.
- On 2026-06-05, after the stage-card height adjustments, the frontend build succeeded again with output `frontend/dist/assets/index-BVYDlikh.css` about `19.61 kB` and `frontend/dist/assets/index-BUm0YFvG.js` about `223.95 kB` before gzip.
- On 2026-06-05, the final in-app browser DOM check confirmed that all six stage cards fully contained their content with no remaining internal overflow: each measured `clientHeight = 250` and `scrollHeight = 250` on the current viewport.
- On 2026-06-05, a real validation run was executed through FastAPI `TestClient` on the six-file set [тестовые доки/Aierfuke 05](</C:/Users/Пользователь/Desktop/dynnoCustoms/тестовые доки/Aierfuke 05>) using files `1. Add 05 signed.pdf`, `2. CI -05.pdf`, `3.  PL-05.pdf`, `4. CO.pdf`, `5. COA of industry grade.pdf`, and `6. LED417527A.pdf`; the run produced `run_id b3ad5d7d-ba65-4cbc-84f6-35d4073962aa`, `pack_id 4b065e44-a8bc-4401-88e5-554c42d90e5a`, workflow `status failed`, and summary `27 total`, `8 passed`, `3 failed`, `4 warnings`, `1 needs_review`, `11 skipped`.
- The full response for the 2026-06-05 `Aierfuke 05` run was saved to [.tmp/validation_run_aierfuke_05_20260605.json](/C:/Users/Пользователь/Desktop/dynnoCustoms/.tmp/validation_run_aierfuke_05_20260605.json); the main gaps were buyer/shipper mismatches (`R001`, `R002`), packing-list field gaps (`R010`, `R014`, `R017`, plus dependent skips), missing payment confirmation for prepayment terms (`R018`), and missing COA expiry date (`R021`).
- On 2026-06-05, `backend/src/dynno_customs_api/services/text_extractor.py` was hardened for OCR-noisy real documents: invoice and packing-list extraction now use label-scoped seller/manufacturer/buyer fields, invoice/addendum/contract identifiers tolerate OCR confusables, packing-list extraction now derives weight fields more robustly, and BL/addendum helper logic was expanded for multiline OCR layouts.
- On 2026-06-05, `backend/src/dynno_customs_api/services/rule_engine_runner.py` was updated so string normalization translates OCR-confusable Cyrillic glyphs and `R001`/`R002` can tolerate minor OCR spelling drift through similarity thresholds rather than only exact normalized equality.
- On 2026-06-05, `backend/tests/test_text_extractor.py` and `backend/tests/test_rule_engine_runner.py` were expanded with `Aierfuke 05` OCR regressions covering invoice, addendum, packing list, bill of lading, certificate of origin, and noisy company-name matching; `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `52 passed in 1.67s`.
- On 2026-06-05, the same six-file `Aierfuke 05` validation run was repeated after the extractor/rule changes; it produced `run_id 6fda62a2-cda2-4dd6-9412-729249c485ef`, `pack_id 8c870a7b-7bea-4682-b12c-3f65e4a88a2c`, workflow `status failed`, and summary `27 total`, `21 passed`, `1 failed`, `1 warning`, `0 needs_review`, `4 skipped`.
- The improved 2026-06-05 `Aierfuke 05` rerun response was saved to [.tmp/validation_run_aierfuke_05_20260605_after_mapping.json](/C:/Users/Пользователь/Desktop/dynnoCustoms/.tmp/validation_run_aierfuke_05_20260605_after_mapping.json); remaining non-passed results were `R018` (payment confirmation file truly missing), `R021` and dependent `R023` (COA expiry date absent in OCR/source text), skipped `R003`/`R005` because the pack still has no separate `contract` document, and skipped `R024` because BL gross weight is still not extracted for this sample.
- On 2026-06-07, `backend/src/dynno_customs_api/services/rule_engine_runner.py` was updated so `R021` no longer blocks the workflow when `coa.expiry_date` is absent but `coa.manufacture_date` exists: the rule now returns a warning-level failure instead of an error-level failure, while still passing when `expiry_date` is present.
- On 2026-06-07, `backend/src/dynno_customs_api/services/tesseract_ocr.py` was updated to append embedded PDF text-layer content to OCR raw text for PDF documents, preserving better table text when the PDF already contains machine-readable text that Tesseract drops.
- On 2026-06-07, `backend/src/dynno_customs_api/services/text_extractor.py` was updated again so BL gross-weight extraction now handles PDF text-layer table layouts where cargo rows are split across multiple lines and the gross-weight value appears between package-type and volume rows rather than beside OCR `TOTAL:` markers.
- On 2026-06-07, `backend/tests/test_rule_engine_runner.py`, `backend/tests/test_tesseract_ocr.py`, and `backend/tests/test_text_extractor.py` were expanded to cover the new `R021` warning behavior, PDF embedded-text OCR fallback, and BL gross-weight extraction from split table text; `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `55 passed in 2.09s`.
- On 2026-06-07, `Aierfuke 05` was rerun after the `R021` and BL OCR/extraction fixes; the run produced `run_id cbd7fd82-ee5f-44cc-bb24-9c895d1d4075`, `pack_id 7a087a47-5123-4aa3-b02e-d252e41998af`, workflow `status needs_review`, and summary `27 total`, `22 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `3 skipped`.
- The 2026-06-07 rerun response was saved to [.tmp/validation_run_aierfuke_05_20260607_r021_bl_fix_v2.json](/C:/Users/Пользователь/Desktop/dynnoCustoms/.tmp/validation_run_aierfuke_05_20260607_r021_bl_fix_v2.json); `R024` no longer skips, and the remaining non-passed results are only skipped `R003`/`R005` due the missing separate contract plus warning-level `R018` (payment confirmation missing) and warning-level `R021` (COA expiry date absent while manufacture date exists).
- On 2026-06-08, the OCR/extraction boundary received its first provider-oriented refactor: `backend/src/dynno_customs_api/services/ocr_service.py` now resolves the active OCR provider from `settings.ocr_provider`, and `backend/src/dynno_customs_api/services/tesseract_ocr.py` now emits structured line-level OCR payloads per page in addition to flat `raw_text`.
- On 2026-06-08, `backend/src/dynno_customs_api/models/domain.py`, `backend/src/dynno_customs_api/models/api.py`, and `backend/src/dynno_customs_api/api/routes/document_packs.py` were extended so OCR pages/documents can expose `lines` and `provider_metadata` through persistence and API responses without changing validation-rule behavior.
- On 2026-06-09, `backend/src/dynno_customs_api/services/normalized_document_builder.py` and `backend/src/dynno_customs_api/services/normalization_service.py` were updated so normalization passes the full `OcrDocumentResultRecord` into extraction instead of relying only on `raw_text_ref`; completed OCR results with either persisted raw text, inline `raw_text`, or page `lines` are now eligible for normalization.
- On 2026-06-09, `backend/src/dynno_customs_api/services/text_extractor.py` gained a first `line-aware` source layer (`OcrTextSource`) plus label-based company extraction for invoice and packing-list roles, so line-only OCR payloads can populate manufacturer/shipper/buyer fields even when no raw OCR text file is present.
- On 2026-06-09, the line-aware packing-list pass was extended further: invoice/packing labeled role extraction now scans multiline text instead of only first OCR lines, prefers the strongest later candidate when duplicate OCR and embedded PDF text coexist, and uses the same label-driven fallback for `S/C NO` contract extraction.
- On 2026-06-09, the line-aware cleanup was extended again for invoice and MBL: invoice `payment_terms` now prefers the last clean `PAYMENT:` label when raw OCR and embedded PDF text both exist, and `backend/src/dynno_customs_api/services/text_extractor.py` now chooses the strongest bill-of-lading shipper/consignee candidate across the full multiline source instead of returning the first noisy OCR hit.
- On 2026-06-08, the new `backend/tests/test_ocr_service.py` plus updates in `backend/tests/test_tesseract_ocr.py` and `backend/tests/test_document_pack_store.py` covered registry-driven OCR provider selection and persisted line payloads; `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `55 passed in 4.57s`.
- On 2026-06-09, `backend/tests/test_text_extractor.py`, `backend/tests/test_normalized_document_builder.py`, and `backend/tests/test_normalization_service.py` were extended with line-only OCR regressions for invoice role extraction, normalization-path coverage, a packing-list duplicate-layer regression where the clean embedded PDF text must override earlier noisy OCR lines, plus new composite-layer regressions for invoice payment terms and Aierfuke MBL shipper cleanup; `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `61 passed in 3.58s`.
- On 2026-06-09, `Aierfuke 05` was rerun through FastAPI `TestClient` after the first line-aware extraction seam; the run produced `run_id 7aa1af86-9dc9-4b96-910d-9c0906ce7582`, `pack_id d87b1bf7-9209-40bf-ad92-9a020c2a5dfd`, workflow `status needs_review`, and the same summary as the 2026-06-07 baseline: `27 total`, `22 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `3 skipped`.
- The 2026-06-09 rerun response was saved to [.tmp/validation_run_aierfuke_05_20260609_line_aware.json](/C:/Users/Пользователь/Desktop/dynnoCustoms/.tmp/validation_run_aierfuke_05_20260609_line_aware.json); invoice `shipper_name` and `manufacturer_name` now normalize cleanly to `HENAN AIERFUKE CHEMICALS CO., LTD`, while the main remaining gaps are still skipped `R003`/`R005` due the missing separate contract plus warning-level `R018` (payment confirmation missing), warning-level `R021` (COA expiry date absent while manufacture date exists), and noisy packing-list company text (`HENAN ATEBEOKE...`, `ATIERFUKE-SOH`) that does not yet use the cleaner invoice/BL reading.
- On 2026-06-09, `Aierfuke 05` was rerun again after the packing-list line-aware extension; the run produced `run_id fd7f04e0-e61b-442a-9961-70eb604fe174`, `pack_id 294ee3c2-4bb1-4a4d-ab6c-9b9a3146ae34`, workflow `status needs_review`, and the same summary `27 total`, `22 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `3 skipped`.
- The 2026-06-09 packing-list rerun response was saved to [.tmp/validation_run_aierfuke_05_20260609_packing_line_aware.json](/C:/Users/Пользователь/Desktop/dynnoCustoms/.tmp/validation_run_aierfuke_05_20260609_packing_line_aware.json); `packing_list.shipper_name`, `packing_list.manufacturer_name`, and `packing_list.contract_no` now also resolve cleanly to `HENAN AIERFUKE CHEMICALS CO., LTD` and `AIERFUKE-SOH`, leaving the only remaining non-passed rules as skipped `R003`/`R005` plus warning-level `R018` and `R021`.
- On 2026-06-09, `Aierfuke 05` was rerun once more after the invoice/MBL cleanup; the run produced `run_id b6c70272-971d-4d0f-8d23-f1175354769e`, `pack_id d4dab447-7610-4626-9d06-e75430d76039`, workflow `status needs_review`, and the same summary `27 total`, `22 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `3 skipped`.
- The 2026-06-09 invoice/MBL cleanup response was saved to [.tmp/validation_run_aierfuke_05_20260609_bl_invoice_cleanup.json](/C:/Users/Пользователь/Desktop/dynnoCustoms/.tmp/validation_run_aierfuke_05_20260609_bl_invoice_cleanup.json); `invoice.payment_terms` now extracts cleanly as `100% T/T IN ADVANCE.` from the embedded PDF text layer, and `mbl.shipper_name` now resolves cleanly to `HENAN AIERFUKE CHEMICALS CO.,LTD`, so the remaining non-passed results are only the expected business/document conditions: skipped `R003`/`R005` because the pack has no separate contract, warning-level `R018` because payment confirmation is absent, and warning-level `R021` because COA expiry date is absent while manufacture date exists.
- On 2026-06-09, `docs/mvp-readiness-table.md` was added as the current validation-only MVP readiness tracker; it records `Aierfuke 05` as the first baseline (`27 total`, `22 passed`, `0 failed`, `2 warnings`, `3 skipped`), classifies remaining non-passed rules as real document/business conditions rather than current OCR noise, and leaves slots for two next sample-pack runs.
- On 2026-06-09, `Paini 07` was run through FastAPI `TestClient`; the response was saved to `.tmp/validation_run_paini_07_20260609.json`, with `run_id 0ab049ce-8b77-4050-a26c-29c5cfaf60af`, `pack_id e8458d0b-e7d1-4997-a3e0-dd935c318b94`, workflow `status failed`, and summary `27 total`, `5 passed`, `5 failed`, `2 warnings`, `1 needs_review`, `14 skipped`.
- On 2026-06-09, `tianrun 57` was run through FastAPI `TestClient`; the response was saved to `.tmp/validation_run_tianrun_57_20260609.json`, with `run_id 33721269-4013-4303-a0ea-c148b5e93b27`, `pack_id 224a28c9-0851-4e9b-a5cb-4c1bb8335002`, workflow `status needs_review`, and summary `27 total`, `21 passed`, `0 failed`, `1 warning`, `0 needs_review`, `5 skipped`.
- On 2026-06-09, `docs/mvp-readiness-table.md` was updated with `Paini 07` and `tianrun 57` readiness snapshots; `Paini 07` is now the next extraction hardening target, while `tianrun 57` is close to MVP readiness but still has role-mapping, contract/date mapping, and BL gross-weight extraction gaps.
- On 2026-06-09, Paini-class extraction was hardened in `backend/src/dynno_customs_api/services/text_extractor.py`: invoice extraction now handles `PN...` invoice/date layouts, `CONTRACT NO.SPNM-SOH dated...`, `Add07`, `TERMS OF PAYMENT`, and PNA-TAD line items priced in `CNY/KG`; packing-list extraction now handles `DRUMS`, `PALLETS`, clean later quantity candidates, pallet presence, PNA-TAD line items, and standalone shipper/manufacturer company lines; COA extraction now handles `BatchNo` and `AnalysisDate`; BL extraction now handles Paini-style original BL numbers and total-row gross weight.
- On 2026-06-09, `backend/tests/test_text_extractor.py` gained Paini regressions for invoice, packing list, COA, and BL extraction plus clean-candidate behavior for duplicated noisy/embedded text; `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `65 passed in 3.65s`.
- On 2026-06-09, `Paini 07` was rerun after extraction hardening; the response was saved to `.tmp/validation_run_paini_07_20260609_after_extraction_hardening_v3.json`, with `run_id 0ad43bbf-cfa0-4c63-b6e0-f4d386cb3b49`, `pack_id 89ca7f7e-16ba-4f18-a37a-710928a3f769`, workflow `status needs_review`, and summary `27 total`, `16 passed`, `0 failed`, `4 warnings`, `0 needs_review`, `7 skipped`.
- On 2026-06-09, Paini `DRUM + PALLETS` packing logic was updated based on user-provided business knowledge: when pallet weight is absent but drums and pallets are present, `backend/src/dynno_customs_api/services/text_extractor.py` derives empty drum tare as `21.0 kg` and derives pallet tare from the remaining gross-minus-net packaging weight; for `Paini 07` this yields `pallet_weight_kg 15.0`.
- On 2026-06-09, `Paini 07` was rerun after the drum/pallet derivation update; the response was saved to `.tmp/validation_run_paini_07_20260609_after_drum_pallet_defaults.json`, with `run_id 8ea1e81a-ddd0-4e1a-9266-37f66ccd9647`, `pack_id 08c9d46b-7479-4026-ae2e-1df408d972f1`, workflow `status needs_review`, and summary `27 total`, `18 passed`, `0 failed`, `3 warnings`, `0 needs_review`, `6 skipped`; `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `65 passed in 3.77s`.
- On 2026-06-09, `backend/src/dynno_customs_api/services/text_extractor.py` was updated again for bilingual addendum handling: addendum party extraction now searches around `SELLER` / `BUYER` markers instead of returning boilerplate spans, addendum contract dates prefer contract-specific lines, OCR-noisy company candidates are cleaned more defensibly, and payment terms now have a direct `Prepayment N%` fallback.
- On 2026-06-09, `backend/tests/test_text_extractor.py` gained a new `Paini 07` bilingual addendum regression covering seller, buyer, contract/addendum dates, incoterms, and payment terms; `backend\\.venv\\Scripts\\python -m pytest backend\\tests -q` passed with `66 passed in 2.41s`.
- On 2026-06-09, `frontend/src/App.tsx` was updated so shipment-stage status coloring no longer treats skipped checks as full document issues: stage cards now reserve `warning` for real warning-only states, downgrade warning+skipped mixes to `check`, and show `N checks skipped` when no actionable issue exists.
- On 2026-06-09, a production frontend build through repo-local cached Node.js `C:\\Users\\Пользователь\\Desktop\\dynnoCustoms\\.cache\\node\\node-v22.12.0-win-x64\\node.exe` succeeded again and produced `frontend/dist/assets/index-DLHLoqyk.css` about `21.94 kB` and `frontend/dist/assets/index-ceeQQNsa.js` about `229.98 kB` before gzip.
- On 2026-06-09, `Paini 07` was rerun after the addendum and frontend-status fixes; the response was saved to `.tmp/validation_run_paini_07_20260609_addendum_frontend_fix.json`, with `run_id 40923023-4cf0-4064-9338-e45454210f00`, `pack_id ca55f8b7-c910-4e8a-8744-b68bc9f99451`, workflow `status needs_review`, and summary `27 total`, `19 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `6 skipped`; `R002` no longer warns, leaving only warning-level `R018` and `R021` plus skipped contract/BL coverage checks.
- On 2026-06-09, `backend/src/dynno_customs_api/services/text_extractor.py` was adjusted again for operator readability and mixed-script OCR cleanup: lowercase Cyrillic confusable handling now also normalizes `т`, treats lowercase `н` as `n` instead of `h`, and addendum buyer/seller evidence snippets now store only the local extracted segment instead of the full bilingual paragraph.
- On 2026-06-09, `backend/src/dynno_customs_api/services/rule_engine_runner.py` was updated so `R003` and `R005` skipped messages explicitly explain that the separate contract document is missing/unreadable rather than implying the addendum fields were not extracted.
- On 2026-06-09, `frontend/src/App.tsx` extraction-review sorting was updated with field priority so key business fields such as seller/buyer, contract/addendum numbers, and contract/addendum dates surface higher in the extracted-data panel instead of being buried in generic alphabetical order.
- On 2026-06-09, targeted regressions in `backend/tests/test_rule_engine_runner.py` were added for mixed-script string normalization and the clearer `R003` / `R005` skipped messages; `backend\\.venv\\Scripts\\python -m pytest backend\\tests\\test_rule_engine_runner.py backend\\tests\\test_text_extractor.py -q` passed with `39 passed in 1.52s`.
- On 2026-06-09, a fresh production frontend build through repo-local cached Node.js `C:\\Users\\Пользователь\\Desktop\\dynnoCustoms\\.cache\\node\\node-v22.12.0-win-x64\\node.exe` succeeded and produced `frontend/dist/assets/index-DLHLoqyk.css` about `21.94 kB` and `frontend/dist/assets/index-BPb-G-6v.js` about `230.52 kB` before gzip.
- On 2026-06-09, `Paini 07` was rerun after the confusable-copy and field-ordering pass; the response was saved to `.tmp/validation_run_paini_07_20260609_confusable_copy_fix.json`, with `run_id 054ed249-4653-4680-b227-151324f9d57d`, `pack_id 2fce52f3-2fb4-4079-89b0-768e35a566d2`, and the same summary `27 total`, `19 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `6 skipped`; `R003` / `R005` now explicitly explain the missing separate contract document, while addendum seller/buyer evidence snippets are shorter and localized to the extracted line.
- On 2026-06-09, `frontend/src/App.tsx` and `frontend/src/styles.css` were updated so contract-dependent skipped rules (`R003`, `R005`) become shipment-level informational notes instead of document-level yellow states when the pack has no separate `contract` file: addendum/invoice/packing cards now stay green if their own checks pass, active issue lists hide those informational skips, recent alerts omit them, and the UI shows a dedicated `Pack note` explaining that contract-dependent comparisons were not performed.
- On 2026-06-09, a follow-up production frontend build through repo-local cached Node.js `C:\\Users\\Пользователь\\Desktop\\dynnoCustoms\\.cache\\node\\node-v22.12.0-win-x64\\node.exe` succeeded and produced `frontend/dist/assets/index-UVFxPR39.css` about `22.23 kB` and `frontend/dist/assets/index-ClX9uPMZ.js` about `231.63 kB` before gzip.
- On 2026-06-09, `frontend/src/App.tsx` was updated again so missing `payment_confirmation` is treated as a shipment-level optional note when it only triggers warning rule `R018`: the payment card keeps a soft `missing`/`optional upload` message, recent alerts omit it, next-action and KPI progress ignore it, and the UI now renders a dedicated pack note telling the operator that the shipment can still be reviewed without proof of prepayment.
- On 2026-06-09, another production frontend build through the same repo-local cached Node.js succeeded and produced `frontend/dist/assets/index-UVFxPR39.css` about `22.23 kB` and `frontend/dist/assets/index-CEGiPsw9.js` about `233.20 kB` before gzip.
- On 2026-06-10, `Paini 07` was rerun through FastAPI `TestClient` from `тестовые доки/Paini 07`; the response was saved to `.tmp/validation_run_paini_07_20260610_rerun.json`, with `run_id 74359b6d-4e45-4300-85eb-310ce51dbbb4`, `pack_id 4e1c1c36-6bb4-437b-81a1-26d8acb725e8`, workflow `status needs_review`, and summary `27 total`, `19 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `6 skipped`.
- On 2026-06-10, the fresh `Paini 07` rerun still produced stage-level `check` states across addendum, invoice, packing list, COA, and bill of lading because visible non-passed rules remain `R006` (Incoterms comparison skipped due only one extracted value), `R021`/`R023` (COA expiry-date absent), and `R025`/`R026` (BL vs packing-list package quantity / container number missing); only `R003`, `R005`, and `R018` are currently downgraded to shipment-level informational handling.
- On 2026-06-10, `backend/src/dynno_customs_api/services/rule_engine_runner.py` was adjusted so `R006` now treats `addendum` and `invoice` as the required Incoterms pair and treats other documents as optional contributors; when only one required Incoterms value is extracted the rule remains `skipped` with a clearer operator-facing message instead of implying a broader document issue.
- On 2026-06-10, `frontend/src/App.tsx` was updated so skipped `R006` becomes a shipment-level informational note rather than a yellow document-state trigger; the UI now keeps optional-doc Incoterms omissions green, adds a dedicated pack note about deferred Incoterms comparison, and only surfaces `R006` again if it becomes a real invalid-value or mismatch result instead of a skip.
- On 2026-06-10, targeted regression `backend\\tests\\test_rule_engine_runner.py` passed with `13 passed in 1.92s`, and a fresh production frontend build through repo-local cached Node.js succeeded with `frontend/dist/assets/index-DfaOpZwK.js` about `233.96 kB` before gzip.
- On 2026-06-10, `Paini 07` was rerun again after the `R006` policy change; the response was saved to `.tmp/validation_run_paini_07_20260610_rerun_after_r006_policy.json`, with `run_id 2b322dc3-4007-4d7d-8a17-90df070122ab`, `pack_id ed18b360-5d61-454a-bc47-60f4bea2e2bd`, and the same summary `27 total`, `19 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `6 skipped`, but stage status is now `ok` for `addendum` and `invoice`, `check` only for `packing_list`, `coa`, and `bill_of_lading`, and `missing`/optional for `payment_confirmation`.
- On 2026-06-10, `frontend/src/App.tsx` layout order was adjusted again so the document-stage cards render immediately below the KPI summary and the `Pack note` sections render lower on the page after the active detail grid; a follow-up production frontend build through repo-local cached Node.js succeeded with `frontend/dist/assets/index-DpncPKSY.js` about `233.96 kB` before gzip.
- On 2026-06-10, `backend/src/dynno_customs_api/services/rule_engine_runner.py` was updated again so `R023` now emits a clearer redundant-skip message when `coa.expiry_date` or `coa.manufacture_date` is missing instead of using the generic date-compare skip wording; this keeps the real operator-facing signal on `R021` while preserving `R023` as a true comparison only when both dates exist.
- On 2026-06-10, `frontend/src/App.tsx` was updated so skipped `R023` is treated as an informational redundant COA skip and no longer appears as a second visible issue on the `COA` stage when the expiry-date absence is already covered by `R021`.
- On 2026-06-10, targeted regression `backend\\tests\\test_rule_engine_runner.py` passed with `14 passed in 1.86s`, and another production frontend build through repo-local cached Node.js succeeded with `frontend/dist/assets/index-A9syeha6.js` about `234.05 kB` before gzip.
- On 2026-06-10, `Paini 07` was rerun after the `R023` cleanup; the response was saved to `.tmp/validation_run_paini_07_20260610_rerun_after_r023_cleanup.json`, with `run_id 3c1dfa0d-bc8d-4d78-acb4-90f6efba2c13`, `pack_id 268522af-91d3-4d27-8dac-ec383c2a14df`, and the same summary `27 total`, `19 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `6 skipped`, but the `COA` non-passed surface is now effectively reduced to the single real warning `R021` plus hidden informational skip `R023`.
- On 2026-06-10, `backend/src/dynno_customs_api/services/rule_engine_runner.py` was updated so skipped `R025` and `R026` messages explicitly say BL vs packing-list package/container comparison was skipped because values were not extracted from both documents, while real mismatches still remain visible failures.
- On 2026-06-10, `frontend/src/App.tsx` was updated so skipped `R025` and `R026` are treated as shipment-level informational notes: Packing List and Bill of Lading cards stay green when those values are merely unavailable, recent alerts and next action omit those skips, and a `Pack note` explains the partial BL/packing-list comparison.
- On 2026-06-10, targeted regression `backend\\tests\\test_rule_engine_runner.py` passed with `16 passed in 3.09s`, and a production frontend build from `frontend/` through repo-local cached Node.js succeeded with `frontend/dist/assets/index-CvwLsnbQ.js` about `234.80 kB` before gzip.
- On 2026-06-10, `Paini 07` was rerun after the `R025`/`R026` UI cleanup; the response was saved to `.tmp/validation_run_paini_07_20260610_rerun_after_r025_r026_ui_cleanup.json`, with `run_id ed847cb3-248d-40a9-8602-bbb73acc5d94`, `pack_id 4c9a1fab-ac67-41f8-a375-7e934de98208`, workflow `status needs_review`, and the same summary `27 total`, `19 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `6 skipped`; applying the frontend informational policy leaves only `R021` as a visible non-passed rule, while `R003`, `R005`, `R006`, `R018`, `R023`, `R025`, and `R026` are informational.
- On 2026-06-11, the frontend TypeScript plus Vite production build from `frontend/` succeeded with `frontend/dist/assets/index-CvwLsnbQ.js` about `234.80 kB` before gzip.
- On 2026-06-11, three baseline packs were rerun on the current code: `Aierfuke 05` saved to `.tmp/validation_run_aierfuke_05_20260611_baseline.json` with `run_id 09a75ab7-12dc-4ff8-8526-219d5f1ff31b`, `pack_id 3721e2d7-adb9-4ffa-9ac6-14e405d77b8b`, status `needs_review`, summary `27 total`, `22 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `3 skipped`; `Paini 07` saved to `.tmp/validation_run_paini_07_20260611_baseline.json` with `run_id 64f3b2d8-8836-4a21-9c9a-c7255cce3a44`, `pack_id b6f920f4-c53b-4848-a838-a595b2eb0b30`, status `needs_review`, summary `27 total`, `19 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `6 skipped`; `tianrun 57` saved to `.tmp/validation_run_tianrun_57_20260611_baseline.json` with `run_id 6058ed1c-be94-4ab5-a897-c4efd34749b4`, `pack_id 766b9dfb-dcb4-4440-82af-ea62bbb4d17b`, status `failed`, summary `27 total`, `22 passed`, `1 failed`, `1 warning`, `0 needs_review`, `3 skipped`.
- On 2026-06-11, two additional sample packs were tested: `Denkim 03` saved to `.tmp/validation_run_denkim_03_20260611.json` with `run_id 1d1d166d-faf6-4267-a98f-2daa62408120`, `pack_id f379588b-5fd7-400a-9645-ff8d54f78b31`, status `failed`, summary `27 total`, `5 passed`, `5 failed`, `2 warnings`, `1 needs_review`, `14 skipped`; `Hugestone 1` saved to `.tmp/validation_run_hugestone_1_20260611.json` with `run_id 87a5eb5e-281a-4621-81c9-0d56cc2870d5`, `pack_id 9ee7d7c0-7994-4eaf-b123-92dbb1539f15`, status `failed`, summary `27 total`, `8 passed`, `2 failed`, `3 warnings`, `0 needs_review`, `14 skipped`.
- On 2026-06-11, `docs/mvp-readiness-table.md` was updated to the fresh baseline and now records `Denkim 03` and `Hugestone 1` as additional weak-template signals; `docs/production-readiness.md` was added with the current production-pilot scope, verified gates, sample-pack results, blockers, and minimum production requirements.
- On 2026-06-11, `frontend/src/lib/api.ts` was changed so the frontend API base URL comes from `VITE_DYNNO_API_BASE` with `http://localhost:8000/api` as the local fallback; `frontend/src/vite-env.d.ts`, `frontend/.env.example`, and `backend/.env.example` were added for production/pilot configuration. A follow-up frontend TypeScript plus Vite build succeeded with `frontend/dist/assets/index-BRHG6iaX.js` about `234.82 kB` before gzip.
- On 2026-06-11, `tianrun 57` was hardened: BL gross-weight extraction in `backend/src/dynno_customs_api/services/text_extractor.py` now prefers cargo-table `BAGS ... KGS` values over legal-address/INN numeric noise, BL date extraction has an ISO-date fallback, and string normalization in both `text_extractor.py` and `backend/src/dynno_customs_api/services/rule_engine_runner.py` ignores `OOO`/`ООО`/`LLC` legal-form prefixes for party matching.
- On 2026-06-11, regression tests were added for the `tianrun 57` BL cargo-table extraction and legal-form party normalization; targeted backend tests passed with `46 passed in 2.60s`, and normalization-builder tests passed with `6 passed in 0.31s`.
- On 2026-06-11, `tianrun 57` was rerun after the BL gross-weight, BL date, and buyer normalization fixes; the response was saved to `.tmp/validation_run_tianrun_57_20260611_after_bl_gross_buyer_date_fix.json`, with `run_id 8a8dd4fd-2b91-4bc9-a7e9-1ce36e55914e`, `pack_id 1679b7f0-871a-4e30-ab4b-caf33933d012`, workflow `status validated`, and summary `27 total`, `25 passed`, `0 failed`, `0 warnings`, `0 needs_review`, `2 skipped`; only `R003` and `R005` remain skipped because the separate contract document is absent.
- On 2026-06-11, `Denkim 03` was hardened for the first pass: `FreightInvoice.pdf` now classifies as `transport_invoice` instead of core `invoice`; Denkim commercial-invoice extraction now reads `DDI2019000000007`, date `2019-01-03`, contract `DNKM-SOH 2018`, `CIF`, `USD`, total `22140.0`, and a `Polyanionic Cellulose LV` line item; Denkim packing-list extraction now reads `720` bags, `18600.0` gross kg, `18000.0` net kg, `PONU0077754`, `20` pallets, and derived pallet weight `30.0 kg`.
- On 2026-06-11, `R025` was updated so a BL package count can match `packing_list.pallet_quantity` for palletized shipments when the BL counts pallets and the packing list also separately counts bags; this fixed the Denkim BL `20 PALLETS` vs packing-list `720 bags / 20 pallets` false mismatch.
- On 2026-06-11, `Denkim 03` was rerun after the invoice, packing-list, classification, and `R025` fixes; the response was saved to `.tmp/validation_run_denkim_03_20260611_after_invoice_packing_r025_fix.json`, with `run_id a14f1e3f-78dc-406b-bd12-d6f26e6df76c`, `pack_id 247300f6-3dd6-4a06-8e3c-2854946b0a52`, workflow `status failed`, and summary `27 total`, `16 passed`, `3 failed`, `0 warnings`, `0 needs_review`, `8 skipped`; remaining failures are `R014` because empty package weight is not stated and `R019`/`R020` because COA OCR is rotated/poor and does not extract batch or manufacture date.
- On 2026-06-11, targeted Denkim/tianrun/classifier/rule regressions passed with `53 passed in 3.13s`, and the full backend regression suite passed with `78 passed in 4.21s`.
- On 2026-06-18, a direct OCR comparison on `тестовые доки/Denkim 03/Coa (en) 1752-2018.pdf` showed that unrotated OCR stayed unreadable (`confidence 0.5245`, `signal 0`), while the 180/270-degree retries produced readable COA text (`confidence about 0.914`, `signal 7`) including `Lot No 1752/2018` and `Production Date 29.12.2018`.
- On 2026-06-18, `Denkim 03` was rerun end-to-end on an isolated repo-local SQLite DB and saved to `.tmp/validation_run_denkim_03_20260618_current.json`, with `run_id 7acfa431-38ca-41f4-9da2-fe0ca80ee0f5`, `pack_id eec9e66d-91bb-4029-b620-7f3a2d92c165`, workflow `status validated`, and summary `27 total`, `22 passed`, `0 failed`, `0 warnings`, `0 needs_review`, `5 skipped`; `R019`/`R020` now pass and `R014` is an informational skip because empty package tare is absent but palletized gross/net reconciliation still holds.
- On 2026-06-18, `docs/execution-roadmap.md`, `docs/mvp-readiness-table.md`, and `docs/production-readiness.md` were updated so Denkim is no longer tracked as a current COA blocker and `Hugestone 1` becomes the next weak-template hardening priority.
- On 2026-06-18, targeted auth plus validation-run regressions passed with `6 passed in 4.48s`, and the full backend regression suite passed with `85 passed in 6.49s`.
- On 2026-06-18, after adding route gating plus the frontend auth flow, `backend\\.venv\\Scripts\\python.exe -m pytest backend\\tests -q` passed with `87 passed in 7.58s`.
- On 2026-06-18, a production frontend build from `frontend/` through repo-local portable Node `..\\.cache\\node\\node-v22.12.0-win-x64\\node.exe` succeeded and produced `frontend/dist/assets/index-BLpex4-G.css` about `23.52 kB` and `frontend/dist/assets/index-CPWjVY-X.js` about `241.22 kB` before gzip.
- On 2026-06-19, auth registration policy was adjusted for the internal MVP: `backend/src/dynno_customs_api/config.py` now defines `auth_open_registration` (default `True`), `backend/.env.example` documents `DYNNO_AUTH_OPEN_REGISTRATION=false` for tighter deployments, and `backend/src/dynno_customs_api/api/routes/auth.py` now keeps registration open when that flag is enabled instead of forcing a one-time bootstrap-only flow.
- On 2026-06-19, the frontend auth gate in `frontend/src/App.tsx` and `frontend/src/styles.css` was updated so existing workspaces with open registration no longer trap the user on login-only UI: when users already exist and self-registration is enabled, the gate now defaults to sign-in, exposes a `Sign in` / `Create account` toggle, and explains that additional self-registered users are created as `operator`.
- On 2026-06-19, auth regression tests were updated for the new policy, targeted backend tests passed with `9 passed in 1.97s`, the full backend regression suite passed with `88 passed in 9.03s`, and a production frontend build from `frontend/` succeeded with `frontend/dist/assets/index-BrSndGBt.css` about `24.03 kB` and `frontend/dist/assets/index-B01Sc1WI.js` about `242.39 kB` before gzip.
- On 2026-06-19, local runtime verification confirmed that `http://127.0.0.1:8000/api/auth/bootstrap-status` returned `{\"has_users\":true,\"registration_open\":true}` and the local Vite UI on `http://127.0.0.1:5173/` responded with HTTP `200`.
- On 2026-06-19, `backend/src/dynno_customs_api/services/text_extractor.py` was hardened for the current `Hugestone 1` weak template: company cleanup now strips quote-like OCR noise, BL extraction now falls back to `_bill_of_lading_party_field(..., "consignee")`, addendum seller/buyer extraction now supports direct `hereinafter referred to as the SELLER/BUYER` English patterns, packing gross/net extraction now supports `20,160.00 KGS | 20,000.00 KGS`, invoice extraction now recognizes `XANTHAN GUM 20000 KG| RMB16.9/KG ¥ 338,000.00`, packing-line derivation now supports `XANTHAN GUM`, COA date extraction now tolerates noisy markers such as `MANUFACTURE DATE ss:`, BL cargo-description extraction now includes `XANTHAN GUM`, and BL date extraction now supports month-name `SHIPPED ON BOARD` dates.
- On 2026-06-19, `backend/tests/test_text_extractor.py` received focused Hugestone regressions for addendum party extraction, invoice line-item and total extraction, packing gross/net plus line-item derivation, noisy COA date parsing, and BL consignee/cargo/date extraction; targeted extractor tests passed with `36 passed in 1.35s`.
- On 2026-06-19, `Hugestone 1` was rerun end-to-end on a fresh isolated SQLite database and saved to `.tmp/validation_run_hugestone_1_20260619_after_fix.json`, with `run_id ec5d227d-a4c5-429d-8cfd-0a7fe4cf5915`, `pack_id 4bbb3c0b-5f69-444e-8282-3474bfaff51f`, workflow `status needs_review`, and summary `27 total`, `21 passed`, `0 failed`, `2 warnings`, `0 needs_review`, `4 skipped`; compared with the 2026-06-19 pre-fix baseline (`8 passed`, `2 failed`, `3 warnings`, `14 skipped`), Hugestone moved out of the workflow-level `failed` bucket.
- On 2026-06-19, the fresh Hugestone rerun confirmed that invoice buyer/total, packing gross/net, COA manufacture/expiry, and BL buyer/cargo now extract on the live sample; the remaining non-passed surface is now mostly due to absent source data or OCR gaps: no separate contract document (`R003`, `R005`), no payment confirmation document (`R018`), no extracted packing-list / BL container number (`R017`, `R026`), and no BL date in the actual OCR text for this rerun (`R022`).
- On 2026-06-19, the full backend regression suite passed after the Hugestone hardening with `93 passed in 8.56s`.
- On 2026-06-19, the validation policy was tightened for BL/container checks while preserving the contract policy: `R003` and `R005` remain skipped shipment-level informational checks when no separate contract document is included, but `R022` now fails with error severity when a BL document exists without `bl_date` while COA manufacture date is present, and `R026` now fails with error severity when BL and packing list are present but either document lacks `container_no`.
- On 2026-06-19, `frontend/src/App.tsx` was updated so only `R025` remains a shipment-level informational BL/packing skip; `R026` is no longer hidden as informational and missing container-number issues remain visible in document issues, alerts, and next action surfaces.
- On 2026-06-19, targeted rule-engine tests passed with `24 passed in 1.18s`, the full backend suite passed with `96 passed in 9.30s`, and a production frontend build from `frontend/` through repo-local portable Node succeeded with `frontend/dist/assets/index-BrSndGBt.css` about `24.03 kB` and `frontend/dist/assets/index-Bmb4VhyH.js` about `242.32 kB` before gzip.
- On 2026-06-19, `Hugestone 1` was rerun after the BL/container policy pass on a fresh isolated SQLite database and saved to `.tmp/validation_run_hugestone_1_20260619_policy.json`, with `run_id 740b8638-13c9-4284-a03b-9c28f5892e41`, `pack_id 55ad5772-39e8-4a78-9017-09847b563afc`, workflow `status failed`, and summary `27 total`, `21 passed`, `2 failed`, `2 warnings`, `0 needs_review`, `2 skipped`; non-passed results are informational contract skips (`R003`, `R005`), warning-level missing packing-list container/payment confirmation (`R017`, `R018`), and strict BL/container errors (`R022`, `R026`).
- On 2026-06-19, `docs/validation-policy.md` was added and `docs/document-validation-spec.md` was updated so validation authors use the explicit policy that `skipped` is acceptable only when missing information is non-critical; critical missing fields in present final documents must remain visible `failed` issues.
- On 2026-06-20, `R017` was tightened from warning to error in `backend/src/dynno_customs_api/services/rule_engine_runner.py`: missing `packing_list.container_no` is now a strict document issue, while `R018` payment confirmation remains warning-level. `docs/validation-policy.md` and `docs/document-validation-spec.md` were updated to match.
- On 2026-06-20, targeted rule-engine tests passed with `26 passed in 1.35s`, and the full backend suite passed with `98 passed in 9.18s`.
- On 2026-06-20, `Hugestone 1` was rerun after the `R017` policy change on a fresh isolated SQLite database and saved to `.tmp/validation_run_hugestone_1_20260620_r017_policy.json`, with `run_id 2b4359eb-b12f-4f78-a10b-a5e12101835b`, `pack_id dae36e03-588e-4b5d-9cac-98980300b881`, workflow `status failed`, and summary `27 total`, `21 passed`, `3 failed`, `1 warning`, `0 needs_review`, `2 skipped`; non-passed results are informational contract skips (`R003`, `R005`), strict missing container/BL errors (`R017`, `R022`, `R026`), and warning-level missing payment confirmation (`R018`).
- On 2026-06-07, the frontend extraction panel was upgraded from a raw JSON dump to a structured review surface for normalized fields and line items; a production build through repo-local cached Node.js `C:\\Users\\Пользователь\\Desktop\\dynnoCustoms\\.cache\\node\\node-v22.12.0-win-x64\\node.exe` succeeded and produced `frontend/dist/assets/index-DxYlyOV4.css` about `21.11 kB` and `frontend/dist/assets/index-Celtz2Z0.js` about `228.22 kB` before gzip.
- On 2026-06-07, the shipment workspace received a product-state pass for `loading`, `processing`, `empty`, `offline`, and `no-match` flows; a repeat production build through the same repo-local cached Node.js succeeded and produced `frontend/dist/assets/index-DLHLoqyk.css` about `21.94 kB` and `frontend/dist/assets/index-DPilxn08.js` about `229.85 kB` before gzip.
- On 2026-05-22, local backend and frontend servers were available at `http://127.0.0.1:8000/` and `http://127.0.0.1:5173/`; HTTP checks returned `200` for `/api/health` and the Vite UI root.
- `frontend` dependencies were installed with portable Node.js `v22.12.0`; `npm run build` completed successfully and produced `frontend/dist/`.
- After frontend workflow and CORS changes, `backend\\.venv\\Scripts\\python -m pytest backend\\tests` passed with `28 passed`, and `frontend` `npm run build` completed successfully.
- After the review-flow UI changes on 2026-05-06, `frontend` TypeScript build and Vite production build both succeeded when invoked directly through repo-local `node.exe`; the `npm.cmd run build` wrapper returned `Access is denied` in this shell environment, but direct `node` execution completed successfully.
- The Vite dev server was started at `http://127.0.0.1:5173/` and returned HTTP 200; the backend API was restarted at `http://127.0.0.1:8000/health` and returned `status: ok`.

## Open Inputs Needed From User

- Real sample documents or anonymized equivalents.
- Exact validation rules per document and cross-document comparison rules.
- Cost-calculation formula, tariff inputs, and route/TN VED-specific exceptions. User plans to provide FOB formula later.
- Still needs clarification on product-name comparison strictness across invoice/packing list/addendum, numeric tolerances, and buyer vs consignee mapping in BL.
